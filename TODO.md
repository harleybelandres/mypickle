# TODO

- [x] Fix deprecated `withOpacity` usage in `lib/screens/create_tournament_screen.dart` (use `withValues`).

- [x] Fix lints in `lib/screens/home_screen.dart` (remove unnecessary underscores; remove unnecessary string interpolation).

- [x] Fix `use_build_context_synchronously` in `lib/screens/login_screen.dart` (use `context.mounted` guard correctly).

- [x] Remove unused `_db` field in `lib/services/auth_service.dart`.

- [x] Remove unused import `dart:convert` in `lib/services/local_db_service.dart`.

- [x] Clean up `test/widget_test.dart` (remove unused and duplicate imports).

- [ ] Run `flutter analyze` to verify issues are resolved.
- [ ] Run `flutter test` to ensure tests pass.

